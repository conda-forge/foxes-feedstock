{% set name = "foxes" %}
{% set version = "0.7.4.19" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/FraunhoferIWES/foxes/archive/v{{ version }}.tar.gz
  sha256: b797e76bb0c29950ad2fd3d0755cef7ae279a2538ed485703cca3cffb25f96d1

build:
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  build:
    - python
    - matplotlib-base
    - numpy <2
    - pandas
    - xarray
    - dask
    - scipy
    - windrose
    - iwopy
    - pyyaml
  host:
    - python >=3.8
    - pip
  run:
    - python
    - matplotlib-base
    - numpy <2
    - pandas
    - xarray
    - dask
    - distributed
    - scipy
    - windrose
    - iwopy
    - pyyaml
    - netcdf4
    - pyarrow

test:
  imports:
    - foxes
  requires:
    - pip
    - pytest
    - pymoo
  source_files:
    - tests
  commands:
    - pip check
    - pytest tests

about:
  home: https://github.com/FraunhoferIWES/foxes
  summary: Farm Optimization and eXtended yield Evaluation Software
  description: |
    The software foxes is a modular wind farm simulation and wake
    modelling toolbox which is based on engineering wake models. It
    has many applications, for example wind farm optimization,
    wind farm post-construction analysis, wake model studies, and
    wind farm simulations invoking complex model chains.
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - SchmJo
